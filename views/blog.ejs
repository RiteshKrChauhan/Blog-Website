<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %> - Blog</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
</head>

<body class="d-flex flex-column min-vh-100">
    <%- include("partials/header.ejs") %>

    <main class="flex-grow-1">
        <div class="container mt-4">
            <div class="row">
                <div class="col-lg-8 mx-auto">
                    <article>
                        <header class="mb-4">
                            <h1 class="fw-bolder mb-1"><%= blog.title %></h1>
                            <div class="text-muted fst-italic mb-2">
                                Created on <%= new Date(blog.date_created).toLocaleDateString() %>
                                <% if (blog.date_updated) { %>
                                    <br>Last updated on <%= new Date(blog.date_updated).toLocaleDateString() %>
                                <% } %>
                            </div>
                        </header>
                        
                        <section class="mb-5">
                            <p class="fs-5 mb-4" style="white-space: pre-line;"><%= blog.content %></p>
                        </section>
                    </article>
                    
                    <!-- Blog Actions -->
                    <div class="d-flex justify-content-between mb-4">
                        <div>
                            <a href="/" class="btn btn-secondary">← Back to Home</a>
                        </div>
                        <div>
                            <a href="/edit/<%= blog.id %>" class="btn btn-warning me-2">✏️ Edit Blog</a>
                            <form action="/delete/<%= blog.id %>" method="POST" style="display: inline;" 
                                  onsubmit="return confirm('Are you sure you want to delete this blog? This action cannot be undone.')">
                                <button type="submit" class="btn btn-danger me-2">🗑️ Delete Blog</button>
                            </form>
                            <a href="/create" class="btn btn-primary">+ Create New Blog</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include("partials/footer.ejs") %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLlLfGRem5+R9g2FzA8ZGN954O5Q"
        crossorigin="anonymous"></script>
</body>

</html>
